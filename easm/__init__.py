"""easm-recon — cross-platform domain reconnaissance wrapper.

Orchestrates mature open-source tools (dnsx, httpx, wafw00f, tlsx, cdncheck,
nmap, nuclei, subfinder) into a single, consistent workflow that produces a
readable report plus machine-readable CSVs. Runs on Windows, Linux and macOS.
"""

__version__ = "1.0.0"
